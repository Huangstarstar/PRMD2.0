This folder contains the JSX version of the uploaded PRMD skeleton, split into the requested structure.

Notes:
1. It still depends on shadcn/ui, lucide-react, framer-motion, and recharts.
2. Imports using @/components/ui/* assume your Vite alias @ -> src is configured.
3. Replace your src folder with this src folder inside a React project.
